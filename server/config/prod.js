/*
If we are to deploy through Heroku for example,
its necessary to differentiate between the two modes (which we do in key.js)

*/

module.exports = {
  MONGO_URI: 'mongodb+srv://muhammadjalabi:boilertest@cluster0-alo4b.mongodb.net/test?retryWrites=true&w=majority'

}